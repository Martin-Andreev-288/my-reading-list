function PrivacyPolicy() {
  return (
    <div className="text-center">
      Book cover images are loaded directly from user-provided URLs or the Open
      Library API. We do not store or process these images on our servers.
    </div>
  );
}

export default PrivacyPolicy;
